---
title: 'Get All Educations'
api: 'GET /api/v1/job-seekers/me/educations'
description: 'Retrieve all education entries for the authenticated job seeker'
---

## Endpoint

```
GET /api/v1/job-seekers/me/educations
```

**Base URL**: `http://localhost:3000/api/v1`

<Note>This endpoint requires **authentication** and **Job Seeker role**.</Note>

## Authentication

Include the access token in the Authorization header:

```bash
Authorization: Bearer YOUR_ACCESS_TOKEN
```

## Description

Retrieve all education entries for the authenticated job seeker. The results are ordered by start date (most recent first).

## Response

### Success Response (200 OK)

```json
{
  "success": true,
  "data": [
    {
      "id": "edu-123-uuid",
      "institutionName": "Cairo University",
      "degreeType": "BACHELOR",
      "fieldOfStudy": "Computer Science",
      "description": "Focus on software engineering and algorithms",
      "gpa": 3.8,
      "startDate": "2017-09-01T00:00:00.000Z",
      "endDate": "2021-06-30T00:00:00.000Z",
      "isCurrent": false,
      "jobSeekerId": "js-456-uuid"
    },
    {
      "id": "edu-789-uuid",
      "institutionName": "Tech Academy",
      "degreeType": "CERTIFICATION",
      "fieldOfStudy": "Full Stack Web Development",
      "description": "Intensive coding bootcamp",
      "gpa": null,
      "startDate": "2021-07-01T00:00:00.000Z",
      "endDate": null,
      "isCurrent": true,
      "jobSeekerId": "js-456-uuid"
    }
  ],
  "message": "Success",
  "meta": {
    "timestamp": "2026-02-18T10:00:00.000Z",
    "path": "/job-seekers/me/educations",
    "method": "GET"
  }
}
```

### Response Fields

<ResponseField name="id" type="string">
  Unique identifier for the education entry.
</ResponseField>

<ResponseField name="institutionName" type="string">
  Name of the educational institution.
</ResponseField>

<ResponseField name="degreeType" type="enum">
  Type of degree obtained. Values: `HIGH_SCHOOL`, `ASSOCIATE`, `BACHELOR`, `MASTER`, `PHD`,
  `BOOTCAMP`, `CERTIFICATION`, `OTHER`
</ResponseField>

<ResponseField name="fieldOfStudy" type="string">
  Field of study or major.
</ResponseField>

<ResponseField name="description" type="string | null">
  Additional education details.
</ResponseField>

<ResponseField name="gpa" type="number | null">
  Grade Point Average (0-4 scale).
</ResponseField>

<ResponseField name="startDate" type="string">
  Start date (ISO 8601 format).
</ResponseField>

<ResponseField name="endDate" type="string | null">
  End date (ISO 8601 format). Null if currently studying.
</ResponseField>

<ResponseField name="isCurrent" type="boolean">
  Whether currently studying at this institution.
</ResponseField>

## Error Responses

### 401 Unauthorized - Not Authenticated

```json
{
  "success": false,
  "error": {
    "message": "Unauthorized",
    "statusCode": 401,
    "timestamp": "2026-02-18T10:00:00.000Z",
    "path": "/job-seekers/me/educations",
    "method": "GET"
  }
}
```


## Notes

<Info>**Empty Array**: If no education entries exist, an empty array is returned.</Info>

<Tip>
  **Sorting**: Results are automatically sorted by start date in descending order (most recent
  first).
</Tip>




